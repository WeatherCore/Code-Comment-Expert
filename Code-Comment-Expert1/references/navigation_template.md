# ZHIDAO.md 模板（项目导航文档）

> 生成位置：目标项目根目录 `ZHIDAO.md`
> 性质：**推荐阅读顺序**，基于 import 依赖 + 分层启发式 + 入口点推断，非精确数据流图。

```markdown
# 项目导航 · ZHIDAO

> 本文件由 code-comment-expert 自动生成，用于帮助新人快速建立项目全景认知。
> 阅读顺序建议：技术栈 → 目录结构 → 模块职责 → 推荐阅读路径。

## 1. 技术栈
- 语言/框架：<从 pom.xml / build.gradle / requirements.txt 识别>
- 构建工具：<Maven / Gradle / pip / ...>
- 关键中间件：<DB / MQ / 缓存 / RPC ...，如能从 import 推断>

## 2. 目录结构（源码视角）
<由 skeleton.json 的 tree 渲染，省略测试/配置/资源>

## 3. 模块职责
| 模块/包 | 职责一句话 | 关键入口类 |
|--------|-----------|-----------|
| <package> | <做什么业务> | <Class> |

## 4. 依赖流向与推荐阅读路径
- 入口层：<Controller / router / main> —— 从这里开始读
- 业务层：<Service / 领域模型> —— 核心逻辑集中处
- 数据层：<DAO / Repository / ORM> —— 落地存储
- 工具层：<util / common> —— 被多方依赖，最后读

### 推荐顺序（新人路线）
1. 先读本文件，建立地图。
2. 从入口层某个具体接口（如「用户下单」）顺 import 往下追。
3. 遇到不懂的类，看其上方意图级注释；仍不懂标「待确认」。

## 5. 已知盲区（v1 限制）
- Java 泛型/内部类/Lombok 提取可能不全，以源码为准。
- 本导航为结构推断，跨接口/多态的真实调用需结合运行时确认。
```
